/* ready waits for everything on the page to be rendered before it runs everything inside the function */
$(document).ready(function(){

}
                  
/* Examples */
/* Adds a class to .class */
$(document).ready(function(){
    $(".class").addClass("");
}